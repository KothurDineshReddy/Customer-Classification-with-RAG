# OpenAI API key — required when USE_MOCK_LLM=false
OPENAI_API_KEY=sk-...

# Set to true to run without calling the OpenAI API (uses deterministic mock responses)
USE_MOCK_LLM=true

# Root directory for job input/output storage
DATA_DIR=./data
